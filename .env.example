# ADE Configuration — copy to .env and fill values
ADE_DATA_DIR=./data
ADE_LOG_LEVEL=info

# Provider API keys (BYOK — store in OS keychain, not here)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Turso (optional — only if using cloud sync)
# TURSO_DATABASE_URL=
# TURSO_AUTH_TOKEN=
