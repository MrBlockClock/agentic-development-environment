[package]
name = "ade-acp"
version = "0.1.0"
edition = "2021"
description = "ADE as an Agent Client Protocol (ACP) agent — multi-host bridge to Zed and peers"

[dependencies]
ade-core = { path = "../core" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }

# Full ACP runtime (agent-client-protocol crate) lands in M2 spike; stdio scaffold first.
