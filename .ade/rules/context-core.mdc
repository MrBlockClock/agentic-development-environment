---
description: ADE-local agent contract pointers (phases, FreeLLMAPI skill, verify)
---
# ADE Context

- Phases: rule `golden-path` (AUDIT → PLAN → EXECUTE). Do not expand EXECUTE scope.
- Verify: skill `verify-ladder` before claiming done.
- Bulk AI: skill `freellmapi-offload` when available.
- Money paths: rule `money-invariants`. Secrets/generated: deny rules with globs.
- After verified roadmap phase boundaries: rule `phase-summary` (`/summarize`).
