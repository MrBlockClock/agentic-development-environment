---
name: ui-strategy
description: >-
  Points ADE agents at progressive-disclosure UI principles. Use when editing
  desktop/browser UI density, modes, or expand/collapse. Full strategy lives in
  the Cursor global skill ui-strategy; ADE wiring is progressive-ui.
---
# UI strategy (ADE stub)

1. Follow Cursor global skill **ui-strategy** when available (tiers, checklist,
   next-action-first).
2. For this repo’s components and mode table, follow **progressive-ui**.
3. Do not invent a second disclosure pattern — reuse
   `apps/desktop/src/components/ui.tsx`.
