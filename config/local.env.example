# ADE — LOCAL profile. Copy to .env.local (or .env) and fill values.
ADE_ENV=local
ADE_DATA_DIR=./data
# Defaults to debug for local; override if noisy.
# ADE_LOG_LEVEL=debug

# Local is offline-first: leave Turso unset to use the on-disk SQLite DB.
# TURSO_DATABASE_URL=
# TURSO_AUTH_TOKEN=
